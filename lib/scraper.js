// Place wiki quotes related functions here

module.exports = {
	manageInput: function (inputObject) {
		// Returns object with same keys and cleaned up values

		return cleanedInputObject;
	},
	formatQuote: function (quote) {
		// Formats quote for optimal searching
	}, 
	formatAuthorName: function (authorName) {
		// Formats authorName for optimal searching
	},
	getOutput: function () {
		// Returns JSON with results 
	}
}